import { ethers } from "hardhat";

export const FOREVER = 20000000000;
export const MIN_PRICE_DEVIATION = ethers.utils.parseEther("1");
export const MAX_PRICE_DEVIATION = ethers.utils.parseEther("1.5");
